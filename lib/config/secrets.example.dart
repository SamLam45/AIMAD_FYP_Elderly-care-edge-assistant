/// Copy this file to `secrets.dart` and fill in your Picovoice AccessKey.
/// `secrets.dart` is gitignored — never commit real keys.
class Secrets {
  static const String picovoiceAccessKey = 'YOUR_PICOVOICE_ACCESS_KEY';
}
